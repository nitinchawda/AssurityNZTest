{
  "name": "assuritynz-test",
  "version": "1.0.0",
  "description": "Automated API tests for the TM Sandbox Carbon Credits Category endpoint using Playwright with TypeScript.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
